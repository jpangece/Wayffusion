你现在在 xiancaigua/Wayffusion 仓库的 MARL branch 上继续重构。

非常重要：本轮不是继续扩展当前自写的 MPE-like particle backend。

当前代码里虽然有 MPEParticleBackend，但它只是自己写的 MPE-like 粒子动力学，并没有真正调用 OpenAI MPE / MPE2 / PettingZoo MPE 的底层 World / Agent / Entity / step / integrate_state 逻辑。

这不符合当前要求。

当前要求是：

只使用成熟 MPE 底层动力学。
如果不是实际 MPE core，就删除或彻底重构。
不要保留自写 particle integration / contact force / damping / collision backend。
不要保留 MPE-like 作为 fallback。
不要把自写 MPE-like 改个名字继续留下。
不要再说“类似 MPE”。
必须真正使用 MPE / MPE2 / OpenAI MPE core 的底层动力学。

============================================================
0. 本轮核心目标
============================================================

把当前自写的：

envs/waypoint/control/dynamics_backends.py::MPEParticleBackend

彻底替换为真正的 MPE core backend。

目标架构：

Wayffusion policy
  outputs final waypoint [N,2]
    ↓
SafetyLayer
  validates waypoint
    ↓
ActionAdapter
  converts waypoint to MPE physical action
    ↓
MPECoreBackend
  uses actual MPE/MPE2/OpenAI MPE World.step() / Agent / Entity / state / action
    ↓
sync MPE agent states back to Wayffusion UAVState
    ↓
Wayffusion scenario reward / task field / coverage / belief / connectivity

强调：

- Wayffusion 的任务、reward、observation、coverage/belief/connectivity 继续保留。
- 但底层动力学积分、速度更新、阻尼、接触力、碰撞力，不允许自己写。
- 这些必须由 MPE core 的 World.step() 等底层逻辑完成。

============================================================
1. 依赖选择原则
============================================================

优先顺序：

1. 优先使用 mpe2 包。
   - 因为 PettingZoo 文档已经说明 pettingzoo.mpe 已迁移到 mpe2。
   - 在 requirements 中加入 mpe2。
   - 优先从 mpe2 中找到可复用的 core/world/agent/entity 结构。

2. 如果 mpe2 不暴露可复用 core classes，则使用 PettingZoo/MPE 内部 core。
   - 可以通过安装 pettingzoo[mpe] 或 mpe2 后 inspect 包结构。
   - 但不要依赖即将移除的 pettingzoo.mpe 作为长期主方案，除非 mpe2 没有暴露底层 core。

3. 如果 pip package 不暴露 core，只允许 vendor 官方 OpenAI multiagent-particle-envs 的 core.py。
   - 必须放到 third_party/openai_mpe/。
   - 必须保留 LICENSE / attribution / README。
   - vendor 文件必须尽量原样保存。
   - 不允许把 vendor core 改成自己的自写版本。
   - 所有 Wayffusion 适配逻辑写在 wrapper 中，不要直接魔改 third_party core。

不允许：

- 继续使用当前手写 MPEParticleBackend 的积分公式。
- 继续保留自己写的 collision force。
- 继续保留 KinematicPointBackend 作为默认或 fallback。
- 在 backend 里自己写：
  velocity = (1-damping) * velocity + force / mass * dt
  position = position + velocity * dt
  这种 MPE-like integration。
- 自己写 contact_force / penetration / collision response。
- 用“我们实现了 MPE-like”作为完成标准。

============================================================
2. 删除或替换旧自写 backend
============================================================

当前文件：

envs/waypoint/control/dynamics_backends.py

里面的这些内容不符合要求：

- MPEParticleBackend 里自写的 velocity integration
- MPEParticleBackend 里自写的 collision/contact force
- MPEParticleBackend 里自写的 no-fly/geofence projection as physical dynamics
- KinematicPointBackend 旧几何移动 backend

请做以下修改：

1. 删除当前 MPEParticleBackend 的自写实现。
2. 删除 KinematicPointBackend，或者至少不要作为可选 backend 出现在 config/build 函数中。
3. 新增唯一主 backend：

class MPECoreBackend

4. build_dynamics_backend(config) 只允许：

name == "mpe_core"

如果 config 使用：

- mpe_particle
- mpe_like_particle
- kinematic_point

必须 raise ValueError，并提示这些 backend 已被删除，因为项目现在只允许真实 MPE core dynamics。

也就是说，不要再保留：

dynamics_backend:
  name: mpe_particle

默认必须改成：

dynamics_backend:
  name: mpe_core

============================================================
3. 新增 MPECoreBackend
============================================================

新增或重写：

envs/waypoint/control/dynamics_backends.py

保留通用结构：

@dataclass
class DynamicsInfo:
    path_length_delta: np.ndarray
    arrived_mask: np.ndarray
    rejected_mask: np.ndarray
    safety_violation_count: int
    min_pairwise_distance: float
    collision_count: int
    no_fly_violation_count: int
    geofence_violation_count: int
    mean_speed: float
    max_speed_observed: float
    mean_control_norm: float
    disturbance_norm: float
    substeps: int
    info: dict

但是 DynamicsBackend 只实现：

class MPECoreBackend(DynamicsBackend)

MPECoreBackend 必须真实持有 MPE world / agents。

推荐内部字段：

self.mpe_world
self.mpe_agents
self.agent_name_to_mpe_agent
self.dt
self.substeps
self.last_positions
self.last_velocities

MPECoreBackend.reset(world):

1. 创建 MPE World。
2. 创建与 Wayffusion UAV 数量相同的 MPE Agent。
3. 对每个 agent 设置：
   - movable = True
   - collide = True
   - silent = True unless communication needed later
   - size / radius
   - accel
   - max_speed
   - mass
   - state.p_pos = Wayffusion UAV position
   - state.p_vel = Wayffusion UAV velocity
   - action.u = zeros
4. 将 MPE world 的 dt / damping / contact_force / contact_margin 等参数设置为 config 中对应参数。
5. 不要自己手写这些 dynamics 参数的作用，只设置给 MPE world，让 MPE world.step() 使用。
6. 保存映射关系。

MPECoreBackend.step(world, controls, adapter_output, safety_result):

1. 将 Wayffusion UAVState 同步到 MPE agents：
   - MPE agent.state.p_pos = uav.position
   - MPE agent.state.p_vel = uav.velocity

2. 将 adapter_output.controls 转成 MPE physical action：
   - agent.action.u = controls[i]
   - 如果 MPE core action.u 语义是 force / acceleration / normalized force，需要按 MPE core 的要求做 scale。
   - 这个 scale 是 action adapter / wrapper 的职责，但底层积分必须仍由 MPE core.step() 做。

3. 调用 MPE world.step()。
   - 如果需要 substeps，则每个 substep 都调用 MPE world.step()。
   - 不允许自己写 integration loop 替代 MPE world.step()。
   - 可以在每个 substep 前同步 action.u。
   - 可以在每个 substep 后做 Wayffusion 特有的 hard safety projection，但必须记录为 safety correction，不要伪装成 MPE dynamics。

4. 从 MPE agents 同步回 Wayffusion UAVState：
   - uav.position = agent.state.p_pos
   - uav.velocity = agent.state.p_vel
   - uav.last_control = controls[i]
   - uav.path_length += distance moved
   - uav.trajectory.append(position)
   - battery update 可以保留 Wayffusion 自己的任务层消耗逻辑

5. 更新 Wayffusion world:
   - world.step_count += 1
   - world.update_communication_graph()
   - world.accumulate_sensor_footprints()

6. 返回 DynamicsInfo。

注意：

- 位置/速度积分必须来自 MPE world.step()。
- 碰撞/contact force 必须来自 MPE core。
- damping 必须来自 MPE core。
- max speed clipping 必须来自 MPE core。
- 不允许在 MPECoreBackend 中自己重新实现以上内容。

============================================================
4. ActionAdapter 的保留边界
============================================================

ActionAdapter 可以保留，因为 Wayffusion 外部 action 是 waypoint，而 MPE core 接收 physical action。

但 ActionAdapter 不能再承担底层动力学积分责任。

允许保留：

- WaypointVelocityTracker
- WaypointPDTracker
- VelocityToForceAdapter
- DirectAccelerationAdapter

但它们的输出必须明确命名为：

mpe_action_u

或者：

physical_action

不要再叫 force backend 或 dynamics integration。

ActionAdapter 只做：

waypoint -> MPE physical action.u

它可以计算：

- desired_velocity
- proportional velocity tracking control
- PD control
- clipping action.u

但不能更新 position / velocity。

============================================================
5. 配置重构
============================================================

修改：

configs/env/waypoint_missions.yaml

默认改为：

dynamics_backend:
  name: mpe_core
  source: mpe2
  dt: 0.1
  substeps: 10
  damping: 0.25
  contact_force: 100.0
  contact_margin: 0.01
  agent_size: 0.02
  agent_accel: 1.0
  max_speed: 0.08
  action_scale: 1.0

删除或废弃：

dynamics_backend:
  name: mpe_particle

删除：

configs/env/waypoint_missions_kinematic_debug.yaml

或者将其改成 expected-failure / deprecated 文档，不要作为可运行配置存在。

删除或修改：

configs/env/waypoint_missions_pd_adapter_debug.yaml

如果保留，只能切换 adapter，不允许切换到非 MPE backend。

所有 env config 中不得再出现：

- mpe_particle
- mpe_like_particle
- kinematic_point

如果出现，测试应该失败。

============================================================
6. 依赖与 vendor 策略
============================================================

新增：

requirements.txt 或 requirements-server.txt 中加入：

mpe2

如果使用 vendor OpenAI MPE core，则新增：

third_party/openai_mpe/
  LICENSE
  README.md
  core.py
  __init__.py

并新增：

docs/third_party_mpe_source.md

说明：

- 使用哪个 upstream。
- 版本 / commit / package。
- 为什么 vendor。
- 哪些文件原样保留。
- Wayffusion wrapper 在哪里。
- 不要把 vendor core 和 Wayffusion adapter 混在一起。

优先尝试 mpe2 import：

python -c "import mpe2; import pkgutil; print(mpe2.__file__); print([m.name for m in pkgutil.walk_packages(mpe2.__path__, mpe2.__name__+'.') if 'core' in m.name or 'world' in m.name])"

如果找不到 core，再尝试 pettingzoo.mpe 内部结构。

如果仍找不到，再 vendor OpenAI MPE core。

最终必须在代码中能说明：

MPECoreBackend 使用的是：
- mpe2 internal core
或
- pettingzoo.mpe internal core
或
- third_party/openai_mpe/core.py

但绝不能是自己写的 particle backend。

============================================================
7. 测试要求：必须证明不是自写后端
============================================================

新增：

tests/test_mpe_core_backend.py

必须测试：

1. MPECoreBackend 能创建真实 MPE world / agent 对象。
   assert hasattr(backend, "mpe_world")
   assert len(backend.mpe_agents) == num_agents

2. MPECoreBackend.step() 必须调用 MPE world.step()。
   - 使用 monkeypatch / spy 包装 backend.mpe_world.step。
   - 调用 backend.step(...)
   - assert world.step 被调用 substeps 次或至少一次。

3. dynamics_backends.py 不再包含自写 MPEParticleBackend。
   - assert not hasattr(module, "MPEParticleBackend")
   - assert build_dynamics_backend({"name": "mpe_particle"}) raises ValueError
   - assert build_dynamics_backend({"name": "kinematic_point"}) raises ValueError

4. 禁止自写 integration 关键模式。
   可以做文本测试，读取 dynamics_backends.py，确保不存在明显自写积分片段：
   - "(1.0 - self.damping) * velocities"
   - "positions + velocities *"
   - "_collision_forces"
   - "contact_force * penetration"
   这些逻辑只能出现在 third_party/openai_mpe/core.py 或 site-packages，不允许出现在 Wayffusion backend wrapper 中。

5. step 后 Wayffusion UAVState 与 MPE agent state 同步：
   - uav.position == agent.state.p_pos
   - uav.velocity == agent.state.p_vel

6. max_speed / damping / contact 参数来自 MPE core config，并设置到 MPE world / agents。
   不要求在 wrapper 里重新实现。

7. finite:
   - position finite
   - velocity finite
   - DynamicsInfo finite

============================================================
8. 更新旧测试
============================================================

删除或重写：

tests/test_mpe_particle_backend.py

改成：

tests/test_mpe_core_backend.py

所有测试中不允许再 import：

MPEParticleBackend
KinematicPointBackend

更新：

tests/test_waypoint_env_api.py
tests/test_mappo_waypoint_trainer.py
tests/test_waypoint_rendering.py
tests/test_action_adapters.py

确保它们默认使用：

dynamics_backend.name = mpe_core

而不是 mpe_particle。

============================================================
9. validate 脚本更新
============================================================

修改：

scripts/check/validate_waypoint_mappo.py

删除 CLI choices 中的：

--dynamics-backend mpe_particle
--dynamics-backend kinematic_point

只允许：

--dynamics-backend mpe_core

或者直接默认不暴露 backend 选择。

如果用户传：

--dynamics-backend mpe_particle

必须报错，并提示：

mpe_particle was removed because it was a self-written MPE-like backend. Use mpe_core.

summary 中记录：

dynamics_backend: mpe_core
mpe_source: mpe2 / pettingzoo_mpe / third_party_openai_mpe
mpe_world_step_calls
uses_real_mpe_core: true

passed 条件增加：

uses_real_mpe_core == true
mpe_world_step_calls > 0

============================================================
10. README / 文档更新
============================================================

更新 README：

必须明确写：

- Wayffusion no longer uses a self-written MPE-like particle backend.
- The only supported dynamics backend is MPECoreBackend.
- MPECoreBackend calls actual MPE world.step().
- Wayffusion keeps its own task/reward/observation layer, but physical particle dynamics come from MPE core.
- Old MPE-like backend and kinematic backend were removed.

删除或修改之前 README 中：

- MPE-like particle dynamics
- KinematicPointBackend
- debug/ablation kinematic backend
- mpe_particle

新增文档：

docs/mpe_core_backend_zh.md

内容包括：

1. 为什么删除自写 MPE-like backend。
2. 当前使用的 MPE source：
   - mpe2
   - pettingzoo.mpe
   - or third_party/openai_mpe
3. Wayffusion UAVState 如何映射到 MPE Agent。
4. Waypoint adapter 如何映射到 MPE action.u。
5. MPE world.step 如何被调用。
6. 哪些仍是 Wayffusion 自己的：
   - tasks
   - reward
   - task_field
   - coverage/belief grids
   - connectivity graph
   - rendering
7. 哪些必须来自 MPE core：
   - velocity integration
   - damping
   - contact/collision force
   - max_speed clipping
8. 如何验证不是自写后端。

============================================================
11. 删除旧文件 / 清理残留
============================================================

搜索全仓库：

mpe_particle
MPEParticleBackend
mpe_like
KinematicPointBackend
kinematic_point
MPE-like
self-written particle
bounded geometric waypoint step

处理规则：

- 除 migration notes / deprecated note 外，不允许作为可用 backend 出现。
- 不允许配置中使用。
- 不允许 README 中推荐。
- 不允许 validate 脚本接受。
- 不允许 tests import。
- 不允许 build_dynamics_backend 支持。

如果保留历史说明，必须明确标记：

Removed / deprecated / not supported.

============================================================
12. 必须运行的命令
============================================================

完成后运行：

python -m py_compile envs/waypoint/control/dynamics_backends.py
python -m pytest tests/test_mpe_core_backend.py
python -m pytest tests/test_action_adapters.py
python -m pytest tests/test_waypoint_env_api.py
python -m pytest tests/test_candidate_selection_policy.py
python -m pytest tests/test_direct_waypoint_policy.py
python -m pytest tests/test_mappo_waypoint_trainer.py
python -m pytest tests/test_waypoint_rewards.py
python -m pytest tests/test_waypoint_rendering.py

运行 validate：

python scripts/check/validate_waypoint_mappo.py \
  --tasks area_coverage belief_search connectivity_expansion \
  --policy-class candidate_selection_waypoint \
  --dynamics-backend mpe_core \
  --action-adapter waypoint_velocity_tracker \
  --num_agents 3 \
  --total_updates 5 \
  --eval_episodes 2 \
  --record_eval_episodes 1 \
  --record_format gif \
  --headless

运行 direct waypoint smoke：

python scripts/check/validate_waypoint_mappo.py \
  --tasks area_coverage belief_search \
  --policy-class direct_waypoint \
  --dynamics-backend mpe_core \
  --action-adapter waypoint_velocity_tracker \
  --num_agents 3 \
  --total_updates 2 \
  --eval_episodes 1 \
  --record_eval_episodes 1 \
  --record_format gif \
  --headless

负向测试：

python scripts/check/validate_waypoint_mappo.py \
  --tasks area_coverage \
  --policy-class direct_waypoint \
  --dynamics-backend mpe_particle \
  --num_agents 3 \
  --total_updates 1 \
  --eval_episodes 1 \
  --headless

这个命令应该失败，并提示 mpe_particle 已删除。

============================================================
13. 最终回复要求
============================================================

完成后请结构化说明：

1. 是否删除了自写 MPEParticleBackend。
2. 是否删除了 KinematicPointBackend / kinematic_point backend。
3. 新的 MPECoreBackend 使用的真实 MPE source 是：
   - mpe2
   - pettingzoo.mpe
   - third_party/openai_mpe
4. MPE world / agents 在哪里创建。
5. Wayffusion UAVState 如何同步到 MPE agent.state。
6. ActionAdapter 输出如何写入 MPE agent.action.u。
7. MPE world.step() 在哪里被调用。
8. 如何证明不是自写 integration。
9. 哪些旧关键词已清理：
   - mpe_particle
   - MPEParticleBackend
   - kinematic_point
   - KinematicPointBackend
10. pytest 结果。
11. validate candidate-selection 结果。
12. validate direct waypoint 结果。
13. negative test 是否按预期失败。
14. GIF 路径。
15. 如果无法直接使用 mpe2 core，是否 vendor 了 OpenAI MPE core，并说明 license/attribution。
16. 如有未完成内容，明确列出，不要隐藏。